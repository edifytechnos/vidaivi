{
  "name": "vidai-practice-test",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && node scripts/check-help.cjs && vite build && node scripts/build-help.mjs",
    "preview": "vite preview",
    "help": "node scripts/build-help.mjs",
    "check-help": "node scripts/check-help.cjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "katex": "^0.16.47"
  }
}
